import Backbtn from "../components/Backbtn";

const NotFoundPage = () => {
  return (
    <div className="p-5 min-h-100 gap-3 flex flex-col items-center justify-center">
      <h1 className="text-2xl">Page Not Found</h1>
      <Backbtn />
    </div>
  );
};

export default NotFoundPage;
